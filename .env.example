# ============================================================
# AgentSociety Economic Simulation - 环境变量配置模板
# ============================================================
# 使用说明：
# 1. 复制此文件为 .env
#    cp .env.example .env
# 2. 填写你的 API Key 和配置
# 3. .env 文件已被 .gitignore，不会泄露到 GitHub
# ============================================================

# ========== API Keys（至少需要设置一个）==========
# OpenAI API Key（推荐用于生产环境）
OPENAI_API_KEY=

# 阿里云 DashScope API Key（可选，与 OpenAI 二选一）
# DASHSCOPE_API_KEY=

# DeepSeek API Key（可选，用于自定义端点）
# DEEPSEEK_API_KEY=
# BASE_URL=http://your-api-endpoint/v1/

# ========== Qdrant 向量数据库配置 ==========
# Qdrant 模式：remote（使用 Docker）或 local（本地文件）
KB_QDRANT_MODE=remote

# Qdrant 服务器地址（remote 模式）
KB_QDRANT_HOST=localhost
KB_QDRANT_PORT=6333

# Qdrant 本地存储路径（local 模式，如果使用）
# KB_QDRANT_PATH=database/qdrant_data

# ========== Embedding 模型配置 ==========
# Embedding 模型路径（可选，默认使用相对路径）
# 默认路径：model/all-MiniLM-L6-v2
# MODEL_PATH=model/all-MiniLM-L6-v2

# ========== Python 路径配置（可选）==========
# 如果需要添加自定义 Python 路径
# PYTHONPATH=.
