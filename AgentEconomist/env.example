# ============================================================
# Agent Economist 环境变量配置模板
# ============================================================
# 使用说明：
# 1. 复制此文件为 .env
#    cp env.example .env
# 2. 填写你的 API Key 和配置
# 3. .env 文件已被 .gitignore，不会泄露到 GitHub
# ============================================================

# ========== API 配置（必填，三选一）==========
# OpenAI API Key（推荐用于生产环境）
OPENAI_API_KEY=

# 或 DashScope API Key（阿里云）
# DASHSCOPE_API_KEY=

# 或 DeepSeek API Key（自定义端点）
# DEEPSEEK_API_KEY=
# BASE_URL=http://your-api-endpoint:3888/v1/

# ========== LLM 配置 ==========
# 使用的模型名称（根据你的 API 选择）
MODEL=gpt-4
ECONOMIST_LLM_MODEL=gpt-4
ECONOMIST_LLM_TEMPERATURE=0.1

# ========== LangGraph 服务器配置 ==========
LANGGRAPH_PORT=8123
LANGGRAPH_HOST=0.0.0.0

# ========== Agent 线程配置 ==========
ECONOMIST_THREAD_ID=economist-chat-1

# ========== LangSmith 追踪（可选）==========
# 如果需要追踪 Agent 执行过程，设置为 true 并填写 API Key
LANGCHAIN_TRACING_V2=false
# LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
# LANGCHAIN_API_KEY=
# LANGCHAIN_PROJECT=agent-economist

# ========== 项目路径配置 ==========
# 项目根目录（通常是项目所在的绝对路径）
PROJECT_ROOT=/root/project/agentsociety-ecosim
# 仿真二进制文件路径（相对于项目根目录）
SIMULATION_BINARY=./agentsociety_ecosim/center/EcoCenter
# 默认配置模板路径（相对于项目根目录）
DEFAULT_CONFIG_TEMPLATE=./default.yaml

# ========== Qdrant 向量数据库配置 ==========
# Qdrant 模式：remote（Docker）或 local（本地文件）
KB_QDRANT_MODE=remote
# Qdrant 服务器地址
KB_QDRANT_HOST=localhost
KB_QDRANT_PORT=6333
# Qdrant 集合名称
KB_QDRANT_COLLECTION=academic_papers

# ========== Embedding 模型路径（可选）==========
# 如果不设置，将使用默认路径：model/all-MiniLM-L6-v2
# MODEL_PATH=/path/to/embedding/model
